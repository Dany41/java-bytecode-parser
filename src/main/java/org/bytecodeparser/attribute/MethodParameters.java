package org.bytecodeparser.attribute;

public class MethodParameters extends AttributeInfo {
    protected MethodParameters(short attributeNameIndex, int attributeLength) {
        super(attributeNameIndex, attributeLength);
    }
}
