package org.bytecodeparser.attribute;

public class Record extends AttributeInfo {
    protected Record(short attributeNameIndex, int attributeLength) {
        super(attributeNameIndex, attributeLength);
    }
}
